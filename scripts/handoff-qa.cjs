const {chromium}=require('@playwright/test');const fs=require('fs');(async()=>{const out='../qa-evidence/handoff-final';fs.mkdirSync(out,{recursive:true});const browser=await chromium.launch({channel:'chrome',headless:true});const page=await browser.newPage();for(const width of [390,1440]){await page.setViewportSize({width,height:900});await page.goto('http://localhost:3002');await page.locator('.cinema-ready').waitFor();for(const p of [0,2.7,10.9,10.97,11]){await page.evaluate(p=>{const e=document.querySelector('.cinema');scrollTo({top:e.offsetTop+(e.offsetHeight-innerHeight)*p/11,behavior:'instant'})},p);await page.waitForTimeout(650);await page.screenshot({path:`${out}/${width}-${p}.png`})}}await browser.close();})();
