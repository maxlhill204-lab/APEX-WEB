const {chromium}=require('@playwright/test');const fs=require('fs');(async()=>{const browser=await chromium.launch({channel:'chrome',headless:true});const page=await browser.newPage();const checks=[];for(const width of [320,375,390,430,768,1024,1280,1440,1920]){await page.setViewportSize({width,height:900});await page.goto((process.env.BASE_URL||'http://localhost:3002')+'/quote?package=growth');await page.waitForTimeout(400);checks.push(await page.evaluate(()=>({width:innerWidth,scroll:document.documentElement.scrollWidth,selected:document.querySelector('.quote-selection-summary strong').textContent})));}fs.writeFileSync((process.env.QA_OUTPUT_DIR||'../qa-evidence/latest')+'/quote-widths.json',JSON.stringify(checks,null,2));console.log(checks);await browser.close();if(checks.some(x=>x.scroll>x.width))process.exitCode=1})();
